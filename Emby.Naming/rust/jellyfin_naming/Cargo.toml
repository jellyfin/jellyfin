[package]
name = "jellyfin_naming"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
fancy-regex = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
